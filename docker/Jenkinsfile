pipeline {
    agent any

    environment {
        FULL_DOCKER_IMAGE_NAME = 'thevirtualbrain/tvb-nest'
        VERSION_TAG = 'latest'
    }

    stages {
        stage ('Run tests in Python 3') {
            agent {
                docker {
                    image '${FULL_DOCKER_IMAGE_NAME}:${VERSION_TAG}'
                    alwaysPull true
                }
            }
            steps {
                sh '''#!/bin/bash
                    source activate neurosci
                    pytest --cov-config .coveragerc --cov=tvb tvb_nest/tests --cov-branch --cov-report xml:TEST_OUTPUT/coverage_nest.xml --junitxml=TEST_OUTPUT/TEST-NEST-RESULTS.xml

                '''
                junit 'TEST_OUTPUT/TEST-NEST-RESULTS.xml'
                step([$class: 'CoberturaPublisher', autoUpdateHealth: false, autoUpdateStability: false, coberturaReportFile: 'TEST_OUTPUT/coverage_library.xml', failUnhealthy: false, failUnstable: false, maxNumberOfBuilds: 0, onlyStable: false, sourceEncoding: 'ASCII', zoomCoverageChart: false])
            }
        }
    }

    post {
        changed {
            mail to: 'lia.domide@codemart.ro, paula.popa@codemart.ro',
            subject: "Jenkins Pipeline ${currentBuild.fullDisplayName} changed status",
            body: """
                Result: ${currentBuild.result}
                Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]'
                Check console output at ${env.BUILD_URL}"""
        }

        success {
            echo 'Build finished successfully'
        }
    }
}